Qt is used for developing graphical user interfaces (GUIs) and multi-platform applications that run on 
all major desktop platforms and some mobile or embedded platforms (iOS being the largest omission). 
Most GUI programs created with Qt have a native-looking interface, in which case Qt is classified as a widget toolkit. Non-GUI programs can also be developed, such as command-line tools and consoles for servers. 
An example of such a non-GUI program using Qt is the Cutelyst web framework.

Qt is a cross-platform application and UI framework. Using Qt, you can write applications once and deploy them 
across desktop, mobile and embedded operating systems without rewriting the source code. 
Qt is partly C++ and partly native code depending on platform.

The Q_OBJECT macro must appear in the private section of a class definition that declares it's own signals and slots
or that uses other services provided by Qt's meta object system.