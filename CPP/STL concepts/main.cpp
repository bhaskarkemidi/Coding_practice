/* STANDARD TEMPLATE LIBRARY
 A set of c++ template classes that provide generic classes and functions that can be used to implement 
 data structures and algorithms.
 2. It mainly composes of:
   Containers.  //It is the way that stored data is organized in memory eg: array of elements.
   Algorithms.- //proceldures that are applied on Containers to process their data. (searching and sorting element)
   Iterators.  //like pointers (point to element in a element used to iterate).
   Function objects.
*/
#include <iostream>

using namespace std;

int main()
{
/*
1. a set of tools avlailable in c++ programming platforms
2. code quickly,efficiently and generic way
3. stack follows LIFO 
4. **compiletime polymorphism
5. Data types not specified in implementation, but rather in its use 
Ex:
   template <typename T>
   T area(T a, T b)
   {
       T result=a*b;
       return result;
   }
7. Containers :: this is the way of organizing the stored data.
   objects that handle a collection of other objects(elements) implementing a well-defined data structure.
   Eg: vectors,stacks,queues,maps,pairs
6.
*/
    return 0;
}

